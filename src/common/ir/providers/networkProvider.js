import { Provider } from "./provider.js";

export class NetworkProvider extends Provider {
  constructor() {
    super();
  }

  getMicroblock(identifier) {
  }

  setMicroblock(identifier, data) {
  }
}
